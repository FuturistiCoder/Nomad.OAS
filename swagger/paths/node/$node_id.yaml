get:
  summary: queries the status of a client node
  operationId: getNode
  tags:
    - nodes
  parameters:
    - $ref: '../../parameters/path/node_id_.yaml'
  responses:
    '200':
      description: node
      content:
        application/json:
          schema:
            $ref: '../../schemas/Node.yaml'