post:
  summary: stops and reschedules a specific allocation
  operationId: stopAllocation
  tags:
    - allocations
  parameters:
    - $ref: '../../parameters/path/alloc_id.yaml'
  responses:
    '200':
      description: allocation stop response
      content:
        application/json:
          schema:
            $ref: '../../schemas/AllocStopResponse.yaml'