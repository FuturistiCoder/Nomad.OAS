get:
  summary: reads information about a specific allocation
  operationId: getAllocation
  tags:
    - allocations
  parameters:
    - $ref: '../../parameters/path/alloc_id.yaml'
  responses:
    '200':
      description: allocation
      content:
        application/json:
          schema:
            $ref: '../../schemas/Allocation.yaml'